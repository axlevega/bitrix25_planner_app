{
  "name": "bitrix25-planner",
  "private": true,
  "description": "Bitrix25 Planner — планирование нагрузки специалистов",
  "scripts": {
    "build:frontend": "cd frontend && npm run build",
    "deploy": "node scripts/deploy.js"
  },
  "dependencies": {
    "basic-ftp": "^5.0.5"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}
