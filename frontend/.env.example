# Base URL backend API (Vite читает только frontend/.env)
# — Локальный бэкенд: оставьте пустым (запросы пойдут на /api, прокси в vite.config.js → localhost:8000)
# — Удалённый бэкенд: укажите полный URL, например https://your-domain.com/api
VITE_API_BASE_URL=
